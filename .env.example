# PostgreSQL Database Configuration
# Works with any PostgreSQL provider (Azure, AWS RDS, Supabase, Railway, etc.)
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require
DATABASE_URL_POOLED=postgresql://username:password@host:5432/database?sslmode=require

# Optional: Individual database credentials (for tools that need them)
DATABASE_HOST=your-postgres-host.com
DATABASE_NAME=your_database_name
DATABASE_USER=your_username
DATABASE_PASSWORD=your_password

# PostgreSQL Environment Variables (for psql/migrations)
PGHOST=your-postgres-host.com
PGUSER=your_username
PGPORT=5432
PGDATABASE=your_database_name
PGPASSWORD=your_password

# Better Auth Configuration
BETTER_AUTH_SECRET=generate-a-random-32-character-string-here
BETTER_AUTH_URL=http://localhost:3000

# Application Settings
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Email Configuration (Resend)
# Sign up at https://resend.com to get your API key
RESEND_API_KEY=re_your_resend_api_key_here
RESEND_FROM_EMAIL=notifications@yourdomain.com

# Example configurations for different providers:
# 
# Azure PostgreSQL:
# DATABASE_URL=postgresql://user@server:password@server.postgres.database.azure.com:5432/dbname?sslmode=require
#
# AWS RDS:
# DATABASE_URL=postgresql://user:password@instance.region.rds.amazonaws.com:5432/dbname
#
# Supabase:
# DATABASE_URL=postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres
#
# Railway:
# DATABASE_URL=postgresql://postgres:[password]@[host].railway.app:5432/railway
#
# Local PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/turns_management